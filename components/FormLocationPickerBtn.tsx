import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FormLocationPickerBtn = () => {
    return (
        <View>
            <Text>FormLocationPickerBtn</Text>
        </View>
    );
};

export default FormLocationPickerBtn;

const styles = StyleSheet.create({});
